{
  "name": "moxymind",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:openFeTests": "cypress open --project ./cypress/fe --config-file cypress.config.js",
    "cy:runFeTests": "cypress run --project ./cypress/fe --config-file cypress.config.js",
    "cy:openBeTests": "cypress open --project ./cypress/be --config-file cypress.config.js",
    "cy:runBeTests": "cypress run --project ./cypress/be --config-file cypress.config.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ajv": "^8.17.1",
    "cypress": "^14.4.0"
  }
}
